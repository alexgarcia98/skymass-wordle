import { MonoTypeOperatorFunction, Observable, OperatorFunction, ThrottleConfig } from "rxjs";
type Acc<T> = {
    batch: T[];
};
/**
 * Takes one of the time-control observable operators like `debounce` or `audit`
 * and changes them from dropping unused inputs to batching all the inputs into
 * one array.
 *
 * @param operator the operator to together-ify. (eg. `debounceTime(100)`)
 * @returns a new operator you can pipe with
 */
export declare function together<T>(operator: MonoTypeOperatorFunction<Acc<T>>): OperatorFunction<T, T[]>;
/**
 * This is sort of like `scan`, except that it allows for state that is not sent
 * down the observable.
 *
 * @param mapper The code that remaps the data. It needs to output `[nextState, output]`
 */
export declare function mapWithState<State, In, Out>(mapper: (state: void | State, arg: In) => [void | State, Out]): OperatorFunction<In, Out>;
/**
 * Takes all the null/undefined values out of the observable
 */
export declare function dropNullish<T>(): OperatorFunction<T, Exclude<T, void | undefined | null>>;
/**
 * Emits a value from the source as an observable and then ignores subsequent
 * values until the emitted value is subscribed to.
 *
 * @param op
 */
export declare function throttled<T>(config?: ThrottleConfig): OperatorFunction<T, Observable<T>>;
/**
 * Allows for some cleanup code to run on unsubscription.
 *
 * @param onUnsubscribe Called
 * @returns
 */
export declare function tapUnsubscribed<T>(onUnsubscribe: () => void): MonoTypeOperatorFunction<T>;
export declare function refireOn<T>(pulsar: Observable<unknown>): MonoTypeOperatorFunction<T>;
export {};
