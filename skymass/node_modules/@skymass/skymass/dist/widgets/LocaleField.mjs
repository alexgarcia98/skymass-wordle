import { z } from "zod";
import { INVALID_VALUE } from "../AppData.mjs";
const zLocaleInfo = z.object({
    timeZone: z.string(),
    utcOffset: z.number(),
    isDST: z.boolean(),
    locale: z.string(),
});
export function Locale(define) {
    return define("Locale")
        .props(z.object({}))
        .shared({
        val: zLocaleInfo.nullish(),
    })
        .defaults(() => ({
        val: undefined,
    }))
        .transform({
        val(_props, localeInfo) {
            return localeInfo !== null && localeInfo !== void 0 ? localeInfo : INVALID_VALUE;
        },
    })
        .render(() => ({}));
}
