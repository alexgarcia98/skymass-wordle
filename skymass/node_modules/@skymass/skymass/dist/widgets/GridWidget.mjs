import { Table, } from "./TableWidget.mjs";
import { BaseWidget } from "./Widget.mjs";
export class Grid extends BaseWidget {
    constructor(capture) {
        const table = new Table(capture, "grid");
        super(table.internal_builder);
        this.table = table;
    }
    get selection() {
        return this.table.selection;
    }
    get hasSelection() {
        return this.table.hasSelection;
    }
    get didSelectionChange() {
        return this.table.didSelectionChange;
    }
    get didSelect() {
        return this.table.didSelect;
    }
    get didUnselect() {
        return this.table.didUnselect;
    }
    get didItemAction() {
        const result = this.table.didRowAction;
        return result ? { action: result.action, item: result.row } : null;
    }
    get didToolbarAction() {
        return this.table.didToolbarAction;
    }
    get didItemClick() {
        const result = this.table.didRowClick;
        return result ? { field: result.col, item: result.row } : null;
    }
}
