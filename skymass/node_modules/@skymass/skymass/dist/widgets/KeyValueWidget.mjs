import { z } from "zod";
import { zRenderFormats } from "../Renderable.mjs";
import { wrapDynamicDataWithProcessingToValue, zLiteralOrPromiseOrObs, } from "./InputField.mjs";
import { DynamicDataWithProcessing, maybeObservable } from "../DynamicData.mjs";
import { zRecordOfAny } from "../Builder.mjs";
import { SkyMassUIError } from "../SkyMassUIError.mjs";
const zField = z.object({
    label: z.string().optional(),
    format: z
        .enum([
        ...zRenderFormats.options,
        "multiline",
        "paragraph",
        "image",
        "avatar",
        "profile",
        "icon",
        "icon_label",
        "",
    ])
        .nullish(),
    hidden: z.boolean().optional(),
});
const zKeyValueProps = z.object({
    label: z.string().nullish(),
    value: zLiteralOrPromiseOrObs(zRecordOfAny).nullish(),
    fields: z.record(zField).nullish(),
    appearance: z.enum(["card", "well", ""]).nullish(),
    size: z.enum(["s", "m", "l", "rows", "stretch"]).optional(),
});
export function KeyValue(define) {
    return define("KeyValue")
        .props(zKeyValueProps)
        .render((props, _, stack) => {
        const v = props.value;
        const value = v instanceof DynamicDataWithProcessing
            ? wrapDynamicDataWithProcessingToValue(v, (data) => {
                if (typeof data !== "object") {
                    throw new SkyMassUIError("<widget>.value needs to be an object", {
                        widgetId: props.id,
                        methodName: "keyValue",
                        stack,
                    });
                }
                return data;
            })
            : maybeObservable(v) || { pending: false, val: v };
        return {
            label: props.label,
            value,
            appearance: props.appearance,
            fields: props.fields,
            size: props.size,
        };
    });
}
