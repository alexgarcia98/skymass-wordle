import { z } from "zod";
export function Row(define) {
    return define("LayoutRow")
        .props(z.object({
        children: z.array(z.any()),
    }))
        .render((props) => ({ children: props.children }));
}
