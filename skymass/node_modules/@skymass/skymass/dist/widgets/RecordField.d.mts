import { Observable } from "rxjs";
import { DynamicData } from "../DynamicData.mjs";
import { MarkDownContent } from "../other.mjs";
import { BaseWidget, Field, WidgetCapture } from "./Widget.mjs";
export type RecordFields<T extends {}> = {
    [Key in keyof T]: Field<T[Key]> & BaseWidget<any, any>;
};
export type RecordFieldProps<T extends {}> = {
    display?: MarkDownContent;
    defaultVal?: T;
};
type Internal<T> = {
    fields: DynamicData<T>;
    reset: () => void;
    setVal: (v: T) => void;
};
export declare class RecordField<T extends {}> extends BaseWidget<RecordFieldProps<T>, Internal<T>> implements Field<T> {
    constructor(capture: WidgetCapture);
    get isReady(): boolean;
    get val(): T;
    reset(): void;
    setVal(vals: T): void;
    asObservable(): Observable<void | T>;
}
export {};
