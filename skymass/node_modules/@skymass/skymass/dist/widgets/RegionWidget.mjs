import { distinctUntilChanged, map, pipe, Subject, switchAll, take, } from "rxjs";
import { z } from "zod";
import { zWidgetId } from "../Builder.mjs";
import { REGION_WIDGET_TYPE } from "../RegionSession.mjs";
import { BaseWidget } from "./Widget.mjs";
import { zRenderable } from "../Renderable.mjs";
const zRegionWidgetProps = z.object({
    id: zWidgetId,
    code: z
        .function()
        .args(z.any(), z.any())
        .returns(z.promise(z.any()).or(z.any())),
    arg: z.any(),
    isForm: z.boolean().default(false).optional(),
    title: zRenderable.nullish().optional(),
    variant: z
        .enum(["sidebar", "palette", "accordion", "carousel"])
        .optional(),
    appearance: z.enum(["card", "well", "inverse"]).optional(),
    size: z
        .enum(["s", "m", "l", "stretch"])
        .nullish()
        .optional(),
    scrollBehavior: z.enum(["tail", "slide", "fade"]).optional(),
    defaultOpen: z.boolean().default(false).optional(),
    group: z.string().optional(),
});
export class RegionWidget extends BaseWidget {
    constructor(capture) {
        super(capture.makeBuilder(REGION_WIDGET_TYPE, zRegionWidgetProps, (context, props, params, state) => {
            // RegionSession will internally convert these widgets into active
            // regions.
            const emits = (state === null || state === void 0 ? void 0 : state.emits) || new Subject();
            return {
                internals: {
                    // any cast necessary because the code prop
                    state: Object.assign(Object.assign({}, props), { emits }),
                    // RegionSession will pass in the childs observable
                    valData: context.declareLocal("region", regionValOp, emits),
                },
                render: null, // RegionSession will fill this in
            };
        }));
    }
    get isReady() {
        return this.internal_builder(".isReady").valData.isReady;
    }
    get val() {
        return this.internal_builder(".val").valData.val;
    }
}
function regionValOp() {
    return pipe(take(1), switchAll(), distinctUntilChanged(), switchAll(), distinctUntilChanged(([prev], [current, comparator]) => comparator(prev, current)), map(([current]) => current));
}
