import { z } from "zod";
const zErrorFieldProps = z.object({
    error: z.string(),
    info: z.string().optional(),
});
export function ErrorField(define) {
    return define("Boolean")
        .props(zErrorFieldProps)
        .render((props) => ({
        error: props.error,
        info: props.info,
    }));
}
