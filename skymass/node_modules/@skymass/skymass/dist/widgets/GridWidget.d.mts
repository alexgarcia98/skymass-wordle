import { SerializableObject } from "../protocol.mjs";
import { Columns, TableProps, Internals as TableInternals } from "./TableWidget.mjs";
import { BaseWidget, WidgetCapture } from "./Widget.mjs";
export type GridProps<ItemType> = Omit<TableProps<ItemType>, "rows" | "cols"> & {
    items: TableProps<ItemType>["rows"];
    fields?: Fields<ItemType>;
};
type Fields<ItemType> = Columns<ItemType>;
export declare class Grid<ItemType extends SerializableObject> extends BaseWidget<GridProps<ItemType>, TableInternals> {
    private table;
    constructor(capture: WidgetCapture);
    get selection(): ItemType[];
    get hasSelection(): boolean;
    get didSelectionChange(): boolean;
    get didSelect(): boolean;
    get didUnselect(): boolean;
    get didItemAction(): {
        action: string;
        item: ItemType;
    } | null;
    get didToolbarAction(): string | null;
    get didItemClick(): {
        item: ItemType;
        field: string;
    } | null;
}
export {};
