/**
 * Sometimes you want to give various things an id, so you do something like
 * `'update' + index`. However, that comes with the minor downside that
 * `update2` will be sorted after `update19` if you were to sort them in a list.
 *
 * This code makes it so that the ids will maintain their lexicographic order.
 *
 * @param prefix The start of every id, usually a letter or something
 * descriptive
 * @param index The index to turn into an id
 * @returns the (prefix + order-preserving-hex)
 */
export declare function orderPreservingId(prefix: string, index: number): string;
export declare class Resolvable<T> {
    readonly promise: Promise<T>;
    private readonly resolver;
    private called;
    constructor();
    resolveWasCalled(): boolean;
    resolve(result: Promise<T> | T): void;
    reject(error: unknown): void;
}
export declare function areBytesEqual(a: undefined | Uint8Array, b: undefined | Uint8Array): boolean;
export declare function deepEqual(a: unknown, b: unknown): boolean;
export declare function isNonValWidget(a: unknown): boolean;
type Val = {
    isReady: boolean;
    val: unknown;
};
export declare function isVal(a: unknown): a is Val;
export {};
