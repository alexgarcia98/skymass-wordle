import { RegionCode } from "./api.mjs";
import { DeclaredWidget, Truthy } from "./AppData.mjs";
import { RegionSession } from "./RegionSession.mjs";
import { RenderElement } from "./protocol.mjs";
export type RenderRoundResult = Readonly<{
    /**
     * The value passed to ui.close()
     */
    result: undefined | Truthy<unknown>;
    /**
     * The tree of all the widgets
     */
    widgets: Record<string, DeclaredWidget>;
    /**
     * The individual elements that need to be rendered, in the top-to-bottom order
     * that they should be rendered. We currently allow markdown, raw strings, and
     * widgets (though the widgets are only "placeholders" because the markdown may
     * move where the widget actually renders).
     */
    body: RenderElement[];
}>;
/**
 * Performs a single render pass on a region
 */
export declare function runRenderRound(regionSession: RegionSession, referencedIds: Set<string>, code: RegionCode<any>, arg: any): Promise<RenderRoundResult>;
