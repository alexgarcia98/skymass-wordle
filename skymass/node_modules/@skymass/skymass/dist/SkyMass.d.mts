import { RegionCode } from "./api.mjs";
type SkyMassConfig = {
    /**
     * This will be a public key identifying your codebase with the SkyMass UI
     * Server.
     */
    key: string;
    /**
     * For testing the experience in slower networks, you can add a delay (in
     * milliseconds) to all communications.
     */
    debugDelay?: number;
    /**
     * The (websocket) host of the SkyMass UI server.
     *
     * You can use this field to connect to an alternate SkyMass UI server,
     * (eg: your.custom-domain.com or beta.skymass.dev)
     *
     * If this field is not defined, it will default to  process.env.SKYMASS_SERVER
     * If that is not defined, it will default to wss://skymass.dev
     */
    server?: string;
};
export declare class SkyMass {
    private _manager;
    constructor(config: Readonly<SkyMassConfig>);
    /**
     * Publishes a page at the given path.
     *
     * @param path The path of the page (eg. `"/example"`)
     * @param code The code to call for running the page
     */
    page(path: string, code: RegionCode<void>): void;
}
export declare function validatePath(path: string): boolean;
export {};
