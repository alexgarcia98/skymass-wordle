/**
 * The manager file contains a singleton that manages connection to the
 * websocket.
 * @file
 */
import { Subscription } from "rxjs";
import { Session } from "./Session.mjs";
import { TypedSocket } from "./socket.mjs";
import { P2SMsg } from "./protocol.mjs";
import { UI } from "./api.mjs";
export type PageInfo = {
    path: string;
    code: (ui: UI) => Promise<void> | void;
};
export declare const serializeConsumer: (arg: unknown) => ArrayBuffer;
export declare const deserializeConsumer: (arg: Uint8Array) => any;
export declare class ProducerManager {
    socketSub: Subscription;
    activeSocket: null | TypedSocket<P2SMsg>;
    user: string;
    debugDelay: number;
    hasReported: boolean;
    processId: null | string;
    readonly sessions: Map<string, Session>;
    private readonly pages;
    private urlPrefix;
    constructor(user: string, debugDelay: number, socketDomain: string);
    private handleSocketOpened;
    private handleSocketClosed;
    private handleMetaMsg;
    private handleConsumerMsg;
    pageDefined(page: PageInfo): void;
}
