import { Observable, Observer, OperatorFunction, Subscription } from "rxjs";
export declare class Store<T, Action = T> {
    private readonly _subject;
    private readonly _observable;
    private _subscription;
    constructor(op: OperatorFunction<Action, T>);
    update(input: Action): void;
    map<R>(mapper: (state: T) => R): Observable<R>;
    asObservable(): Observable<T>;
    subscribe(observer: (val: T) => void | Partial<Observer<T>>): Subscription;
}
export declare function createValueStore<T>(initialState: T): Store<T, (prev: T) => T>;
export declare function createAsyncStore<T>(initialState: () => T | Promise<T>): Store<T, (prev: T) => T | Promise<T>>;
export declare function createActionStore<Action, State>(reducer: (state: State, action: Action) => State, initialState: State): Store<State, Action>;
export declare function createAsyncActionStore<Action, State>(reducer: (state: State, action: Action) => State | Promise<State>, initialState: State | Promise<State>): Store<State, Action | Promise<Action>>;
export declare function createJSONStore<State>(filename: string, initialState: State): Store<State, (prev: State) => State | Promise<State>>;
