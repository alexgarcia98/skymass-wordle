var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _UI_internals;
import { z } from "zod";
import { OMIT_VALUE } from "./AppData.mjs";
import { md } from "./other.mjs";
import { WidgetCapture, } from "./widgets/Widget.mjs";
import { AsyncField } from "./widgets/AsyncField.mjs";
import { OneOfField, RadioGroupField, SelectField, } from "./widgets/OneOfField.mjs";
import { SearchableSelect } from "./widgets/SearchableSelect.mjs";
import { RecordField, } from "./widgets/RecordField.mjs";
import { Button, CloseButton, NavButton } from "./widgets/ButtonWidget.mjs";
import { Link } from "./widgets/LinkWidget.mjs";
import { RegionWidget } from "./widgets/RegionWidget.mjs";
import { Table } from "./widgets/TableWidget.mjs";
import { Grid } from "./widgets/GridWidget.mjs";
import { RegionType, LogLevel, } from "./protocol.mjs";
import { DynamicData, DynamicDataWithProcessing, forceObservable, rxToPromise, } from "./DynamicData.mjs";
import { Form } from "./widgets/Form.mjs";
import { EmailField } from "./widgets/EmailField.mjs";
import { NumberField } from "./widgets/NumberField.mjs";
import { Boolean } from "./widgets/BooleanField.mjs";
import { CheckboxGroupField, ComboBoxField, MultiSelectField, SomeOfField, } from "./widgets/SomeOfField.mjs";
import { ImageWidget } from "./widgets/ImageWidget.mjs";
import { IFrameWidget } from "./widgets/IFrameWidget.mjs";
import { Gallery } from "./widgets/GalleryWidget.mjs";
import { ColorField } from "./widgets/ColorField.mjs";
import { VideoWidget } from "./widgets/VideoWidget.mjs";
import { AudioWidget } from "./widgets/AudioWidget.mjs";
import { DateField } from "./widgets/DateField.mjs";
import { DateTimeField } from "./widgets/DateTimeField.mjs";
import { TimeField } from "./widgets/TimeField.mjs";
import { fromEventPattern, mergeMap, noop, pipe, take, } from "rxjs";
import { deepEqual, isNonValWidget, orderPreservingId } from "./utils.mjs";
import { makeWidget } from "./widgets/makeWidget.mjs";
import { String } from "./widgets/StringField.mjs";
import { Password } from "./widgets/PasswordField.mjs";
import { TextAreaField } from "./widgets/TextAreaField.mjs";
import { Confirm } from "./widgets/Confirm.mjs";
import { Alert } from "./widgets/Alert.mjs";
import { LineChart } from "./widgets/LineChart.mjs";
import { BarChart } from "./widgets/BarChart.mjs";
import { PieChart } from "./widgets/PieChart.mjs";
import { ScatterChart } from "./widgets/ScatterChart.mjs";
import { EChartsWidget } from "./widgets/EChartsWidget.mjs";
import { Nav } from "./widgets/Nav.mjs";
import { RangeField } from "./widgets/RangeField.mjs";
import { ProgressBar } from "./widgets/ProgressBar.mjs";
import { Txt } from "./widgets/TxtWidget.mjs";
import { KeyValue } from "./widgets/KeyValueWidget.mjs";
import { URLField } from "./widgets/URLField.mjs";
import { MenuBar } from "./widgets/MenuBar.mjs";
import { Theme } from "./widgets/Theme.mjs";
import { Breadcrumb } from "./widgets/Breadcrumb.mjs";
import { InfoBar } from "./widgets/InfoBar.mjs";
import { CodeWidget } from "./widgets/CodeWidget.mjs";
import { CodeMirrorWidget } from "./widgets/CodeMirrorWidget.mjs";
import { PageParams } from "./widgets/PageParams.mjs";
import { History, PreventExit } from "./widgets/History.mjs";
import { FileUpload, DropZone, S3FileUpload, S3DropZone, } from "./widgets/FileUploadField.mjs";
import { LocationSearch } from "./widgets/LocationSearchField.mjs";
import { GoogleMaps } from "./widgets/GoogleMaps.mjs";
import { Joystick } from "./widgets/Joystick.mjs";
import { InfoIcon } from "./widgets/InfoIcon.mjs";
import { DownloadWidget } from "./widgets/DownloadWidget.mjs";
import { StatsWidget } from "./widgets/StatsWidget.mjs";
import { Calendar } from "./widgets/CalendarWidget.mjs";
import { NavBar, zRouteMap, zRoute, } from "./widgets/NavBar.mjs";
import { assertParseSucceeded, zWidgetId } from "./Builder.mjs";
import { SkyMassUIError } from "./SkyMassUIError.mjs";
import { QueryParam } from "./widgets/QueryParamField.mjs";
import { GeoLocation } from "./widgets/GeoLocationField.mjs";
import { Locale } from "./widgets/LocaleField.mjs";
import { CloseWindow } from "./widgets/CloseWindow.mjs";
// temporary global map, will make it specific to each RegionSession
let nextSubscribableId = 0;
const subscribablesMap = new WeakMap();
const EMPTY_LIST = [];
const INITIAL_VALUE = Symbol("initial_value");
/**
 * This is the data that is passed to user code on each render.
 */
export class UI {
    constructor(internals) {
        _UI_internals.set(this, void 0);
        __classPrivateFieldSet(this, _UI_internals, internals, "f");
    }
    throwIfExpired(widgetId = null) {
        if (__classPrivateFieldGet(this, _UI_internals, "f").expired) {
            throw new SkyMassUIError(`<widget> called after render loop completed`, {
                widgetId,
            });
        }
        // TODO: if a click causes and the parent session no longer renders us, this will complain
        // if (this.#internals.regionSession.isComplete) {
        //   throw new SkyMassUIError(`<widget> called after session terminated`, {
        //     widgetId,
        //   });
        // }
    }
    user() {
        return __classPrivateFieldGet(this, _UI_internals, "f").user;
    }
    async(id, code, dependencies) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([{ id, code, dependencies }]);
        return new AsyncField(widgetCapture);
    }
    /**
     * Takes the value and outputs it to the parent.
     * This must not run every render, or it may trigger excess re-renders.
     *
     * @param val
     * @param comparator comparator, defaulted to deepEqual
     */
    emit(val, comparator = deepEqual) {
        __classPrivateFieldGet(this, _UI_internals, "f").regionSession.emits.next([val, comparator]);
    }
    /**
     * Pass a function and an array of dependencies. ui.memo will only
     * recompute the memoized value by calling your function when any
     * of the dependencies has changed. Returns an object representing the most recent
     * value returned from your function.  Promises are awaited.
     *
     * The dependency param can be:
     * array of values -- shallowly compared.  It handles .val's automatically.
     * [] - only compute the first time
     *
     * @param id    An unique identifier
     * @param code  The function to run to produce the value
     * @param dependencies An optional array of dependencies.
     * @param initialVal An optional initial value.
     * @returns An object with .isReady and .val
     **/
    memo(id, code, dependencies = [], initialVal) {
        this.throwIfExpired(id);
        const { regionSession } = __classPrivateFieldGet(this, _UI_internals, "f");
        assertParseSucceeded(zMemoProps.safeParse({ id, code, dependencies, initialVal }), id);
        const { referencedIds, regionVals } = regionSession;
        const key = regionVals.declareMemoWithProcessing(id, code, dependencies, initialVal);
        let dd = regionVals.ddCache.get(key);
        if (dd) {
            return dd;
        }
        const obs = regionVals.observe(key, true);
        dd = new DynamicDataWithProcessing(obs, id, () => referencedIds.add(key), () => regionVals.ddCache.delete(key));
        regionVals.ddCache.set(key, dd);
        return dd;
    }
    /**
     * Returns a .val that holds the most recent value from a subscription.
     * The subscription is set up by the "setup" function which is re-run whenever
     * any of the dependencies change.
     *
     * The setup function will be passed an "update" callback which should be called to
     * produce a new value.
     *
     * The setup function should also return a cleanup callback that's
     * called when the session ends or the subscription goes out of scope.
     *
     * @example
     * const latestPosts = ui.subscribe("posts", (update) => {
     *   // a custome event handler.
     *   // calls update with the value to produce
     *   const handler = (event) => update(event.posts);
     *   // register our handler with a source
     *   source.addListener("new_post", handler);
     *   // return a callback to clean up
     *   return () => source.removeListener("new_post", handler);
     * });
     * if (latestPosts.isReady) {
     *   // will be called everytime the "new_post" event fires.
     *   console.log(latestPosts.val);
     * }
     *
     * The dependency param can be:
     * array of values -- shallowly compared.  It handles .val's automatically.
     * [] - only compute once on first render
     *
     * @param id    An unique identifier
     * @param code  The function that sets up the subscription and returns the cleanup function.
     * @param dependencies An array of dependencies.
     * @param transform An optional function to transform the value passed into update to the value available in .val
     * @param initialVal An optional initial value
     **/
    subscribe(id, code, dependencies, transform, initialVal) {
        this.throwIfExpired(id);
        assertParseSucceeded(zSubscribeProps.safeParse({
            id,
            code,
            dependencies,
            transform,
            initialVal,
        }), id);
        const { regionSession } = __classPrivateFieldGet(this, _UI_internals, "f");
        const { referencedIds, regionVals } = regionSession;
        const makeObs = () => fromEventPattern(
        // the function to add the listener and return an unsubscribe function
        (update) => {
            let cancelled = false;
            function isCancelled() {
                return cancelled;
            }
            const newUpdate = (val) => {
                if (cancelled)
                    return;
                update(val);
            };
            const cleanup = code(newUpdate, isCancelled);
            if (!cleanup) {
                console.warn(`ui.subscribe(id = ${id})'s setup function did not return a cleanup function.`);
            }
            return () => {
                cancelled = true;
                cleanup === null || cleanup === void 0 ? void 0 : cleanup();
            };
        }, 
        // The unsub function
        (_, cleanup) => cleanup(), 
        // optional projection
        transform);
        const key = regionVals.declareMemo(id, makeObs, dependencies, initialVal);
        let dd = regionVals.ddCache.get(key);
        if (dd) {
            return dd;
        }
        const obs = regionVals.observe(key, true);
        dd = new DynamicData(obs, id, () => referencedIds.add(key), () => regionVals.ddCache.delete(key));
        regionVals.ddCache.set(key, dd);
        return dd;
    }
    /**
     * Calls the code function every interval milliseconds.  The function can call the update callback to emit a new value and cause a re-render.  The most recent value is available in .val.
     *
     * @example
     * const time = ui.interval("timer", () => Date.now(), [], 1000);
     * ui.txt("time", time);
     *
     * or conditional updates:
     *
     * this.interval(
     *   "poll_db",
     *   async (update, prev, isCancelled) => {
     *     const current = await db.query(
     *       "SELECT * FROM posts WHERE user_id = :user_id",
     *       { user_id }
     *     );
     *     // conditionally update
     *     if (!isCancelled() && current.length !== prev?.length) {
     *       update(current);
     *     }
     *   },
     *   [user_id],
     *   2000
     * );
     *
     * The dependency param can be:
     * array of values -- shallowly compared.  It handles .val's automatically.
     * [] - only compute once on first render
     *
     * @param id    An unique identifier
     * @param code  The function that sets up the subscription and returns the cleanup function.
     * @param dependencies An array of dependencies.
     * @param transform An optional function to transform the value passed into update to the value available in .val
     * @param initialVal An optional initial value
     **/
    interval(id, code, dependencies, interval = 1000, initialVal) {
        this.throwIfExpired(id);
        // TODO: internval > 1000
        // TODO: this should pause when the client is sleep:
        return this.subscribe(id, (update, isCancelled) => {
            let timer = null;
            let prev = null;
            const newUpdate = (val) => {
                prev = val;
                update(val);
            };
            function tick() {
                return __awaiter(this, void 0, void 0, function* () {
                    if (isCancelled())
                        return;
                    yield code(newUpdate, prev, isCancelled);
                    if (isCancelled())
                        return;
                    timer = setTimeout(() => {
                        tick();
                    }, interval);
                });
            }
            tick();
            return () => timer && clearTimeout(timer);
        }, dependencies, undefined, initialVal);
    }
    /**
     * Executes the provided "effect" whenever any of the dependencies change.
     * You can use this to perform side effects like logging,
     * setting up subscriptions, writing to remote systems, etc...
     *
     * The function can return an optional cleanup callback that's
     * called when the session ends or it goes out of scope.
     * You can use it to remove subscriptions or perform other clean up work.
     * Note that ui.effect performs cleanup before it starts a new execution:
     * effect -> dep change -> clean up -> effect -> ...
     *
     * @example
     * ui.effect(
     *   "sync_counters",
     *   () => {
     *     // sync a counter in a remote system
     *     remote.set(counter.val);
     *     // cleanup function
     *     return () => remote.disconnect();
     *   },
     *   [counter]
     * );
     *
     * The dependency param can be:
     * array of values -- shallowly compared.  It handles .val's automatically.
     * [] - only compute once on first render
     *
     * @param id    An unique identifier
     * @param effect  The function that performs the effect (and returns an optional cleanup function)
     * @param dependencies An array of dependencies.
     **/
    effect(id, effect, dependencies) {
        this.throwIfExpired(id);
        assertParseSucceeded(zEffectProps.safeParse({ id, effect, dependencies }), id);
        this.memo("__sm_effect_" + id, () => fromEventPattern((_) => {
            const cleanup = effect();
            if (!cleanup) {
                console.warn(`ui.effect(id = ${id})'s effect function did not return a cleanup function.`);
            }
            return cleanup;
        }, (_, cleanup) => cleanup && cleanup()), dependencies);
    }
    /**
     * Returns the most recent value from an Observable
     *
     * @param subscribable An observable value
     */
    latest(subscribable) {
        return this.readFrom(subscribable);
    }
    /**
     * Returns the most recent value from an Observable
     *
     * Alias for ui.latest
     *
     * @param source An observable value
     */
    readFrom(source) {
        this.throwIfExpired();
        let info = subscribablesMap.get(source);
        if (!info) {
            info = {
                id: orderPreservingId("obs", nextSubscribableId++),
                declared: {
                    code: () => pipe(
                    // just used for timing
                    take(1), mergeMap(() => forceObservable(source))),
                    isEvent: false,
                },
            };
            subscribablesMap.set(source, info);
        }
        // TODO: this leaks
        const { regionSession } = __classPrivateFieldGet(this, _UI_internals, "f");
        const { referencedIds, regionVals } = regionSession;
        const key = regionSession.buildLocal(info.declared, null);
        const cache_key = `dd_${key}`;
        let dd = regionVals.ddCache.get(cache_key);
        if (dd) {
            return dd;
        }
        const obs = regionVals.observe(key, true);
        dd = new DynamicData(regionSession.regionVals.observe(key, true), undefined, () => referencedIds.add(key), () => regionVals.ddCache.delete(cache_key));
        regionVals.ddCache.set(cache_key, dd);
        return dd;
    }
    /**
     * Groups related input fields into an object
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param fields The fields
     * @param props Additional props you may wish to set
     * @returns The created {@link RecordField}
     */
    record(id, fields, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([{ id, fields }, props]);
        return new RecordField(widgetCapture);
    }
    keyValue(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(KeyValue, widgetCapture, { id }, props);
    }
    /**
     * Displays a table of data
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param rows Rows of data
     * @param props Additional props you may wish to set
     * @returns The created {@link Table}
     */
    table(id, rows, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([{ id, rows }, props]);
        return new Table(widgetCapture);
    }
    /**
     * Displays a grid of data
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param items data items
     * @param props Additional props you may wish to set
     * @returns The created {@link Grid}
     */
    grid(id, items, props) {
        this.throwIfExpired("ui.grid");
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([{ id, rows: items }, props]);
        return new Grid(widgetCapture);
    }
    /**
     * Displays a calendar of events data
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param events list of events
     * @param props Additional props you may wish to set
     * @returns The created {@link Calendar}
     */
    calendar(id, events, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([{ id, events }, props]);
        return new Calendar(widgetCapture);
    }
    /**
     * Displays a gallery
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param rows Rows of data
     * @param props Additional props you may wish to set
     * @returns The created {@link Gallery}
     */
    gallery(id, rows, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([{ id, rows }, props]);
        return new Gallery(widgetCapture);
    }
    /**
     * Displays a text input box
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link StringField}
     */
    string(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(String, widgetCapture, { id }, props);
    }
    /**
     * Displays a text input box
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link TextAreaField}
     */
    textarea(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(TextAreaField, widgetCapture, { id }, props);
    }
    /**
     * Displays a number input box
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link NumberField}
     */
    number(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(NumberField, widgetCapture, { id }, props);
    }
    /**
     * Displays a range input
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link RangeField}
     */
    range(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(RangeField, widgetCapture, { id }, props);
    }
    /**
     * Displays a color selection input
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link ColorField}
     */
    color(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(ColorField, widgetCapture, { id }, props);
    }
    /**
     * Displays a stats widget
     */
    stat(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(StatsWidget, widgetCapture, { id }, props);
    }
    /**
     * Displays a URL input
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link URLField}
     */
    url(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(URLField, widgetCapture, { id }, props);
    }
    /**
     * Displays a checkbox
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link BooleanField}
     */
    checkbox(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Boolean, widgetCapture, { id }, props);
    }
    boolean(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Boolean, widgetCapture, { id }, props);
    }
    /**
     * Displays a email text input box
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link EmailField}
     */
    email(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(EmailField, widgetCapture, { id }, props);
    }
    /**
     * Displays a password input box
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link Password}
     */
    password(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Password, widgetCapture, { id }, props);
    }
    /**
     * Displays a date input box
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link DateField}
     */
    date(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(DateField, widgetCapture, { id }, props);
    }
    /**
     * Displays a date time input box
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link DateTimeField}
     */
    datetime(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(DateTimeField, widgetCapture, { id }, props);
    }
    /**
     * Displays a time input box
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link TimeField}
     */
    time(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(TimeField, widgetCapture, { id }, props);
    }
    /**
     * Displays radio group or drop down based on the number of choicse
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link OneOfField}
     */
    one_of(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(OneOfField, widgetCapture, { id }, props);
    }
    /**
     * Displays a radio group
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link RadioGroupField}
     */
    radioGroup(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(RadioGroupField, widgetCapture, { id }, props);
    }
    /**
     * Displays a single select
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link SelectField}
     */
    select(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(SelectField, widgetCapture, { id }, props);
    }
    /**
     * Displays a searchable single select
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link SearchableSelect}
     */
    searchableSelect(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(SearchableSelect, widgetCapture, { id }, props);
    }
    /**
     * Displays a checkbox group or multiselect
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link SomeOfField}
     */
    some_of(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(SomeOfField, widgetCapture, { id }, props);
    }
    /**
     * Displays a checkbox group
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link CheckboxGroupField}
     */
    checkboxGroup(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(CheckboxGroupField, widgetCapture, { id }, props);
    }
    /**
     * Displays a multiple select list
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link MultiSelectField}
     */
    multiSelect(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(MultiSelectField, widgetCapture, { id }, props);
    }
    /**
     * Displays a multiple select comboxbox
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link ComboBoxField}
     */
    comboBox(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(ComboBoxField, widgetCapture, { id }, props);
    }
    /**
     * A file input
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link FileUpload}
     */
    fileUpload(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(FileUpload, widgetCapture, { id }, props);
    }
    /**
     * Upload files to S3 or similar
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link S3FileUpload}
     */
    S3fileUpload(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(S3FileUpload, widgetCapture, { id }, props);
    }
    /**
     *  Allows droppping files
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link DropZone}
     */
    dropZone(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(DropZone, widgetCapture, { id }, props);
    }
    /**
     * Allow dropping files that are then uploaded to S3 or compatible
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link S3dropZone}
     */
    S3dropZone(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(S3DropZone, widgetCapture, { id }, props);
    }
    /**
     * Displays a nav bar which triggers value change when clicked
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link Nav}
     */
    nav(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Nav, widgetCapture, { id }, props);
    }
    /**
     * Displays a nav bar with items that navigate to the given location when clicked
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link NavBar}
     */
    navBar(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(NavBar, widgetCapture, { id }, props);
    }
    /**
     * Get/set a URL query parameter with the given key.
     *
     * If present, .val will be set to its value
     * Otherwise, the URL parameter and .val will be set to defaultVal
     * Calling .setVal(value) will update the paramete and .val
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link QueryParam}
     */
    queryParam(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(QueryParam, widgetCapture, { id }, props);
    }
    /**
     * Displays a button to the user.
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link ButtonWidget}
     */
    button(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Button, widgetCapture, { id }, props);
    }
    /**
     * Displays a submit button to the user.
     *
     * Submit buttons are used with ui.form and only "click" when all inputs are a in valid state.
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link ButtonWidget}
     */
    submitButton(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Button, widgetCapture, { id }, Object.assign(Object.assign({}, props), { validate: true }));
    }
    /**
     * Displays a download button to the user.
     *
     * Download buttons let users trigger downloads from the browser.
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link DownloadWidget}
     */
    downloadButton(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(DownloadWidget, widgetCapture, { id }, props);
    }
    download(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(DownloadWidget, widgetCapture, { id }, props);
    }
    /**
     * Displays a button to the user that, when pressed, will close the region.
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link CloseButton}
     */
    closeButton(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(CloseButton, widgetCapture, { id }, props);
    }
    /**
     * Displays a button to the user that, when pressed, will navigate to the specified route.
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link NavButton}
     */
    navButton(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(NavButton, widgetCapture, { id }, props);
    }
    /**
     * Displays a hyperlink to the user.
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link LinkWidget}
     */
    link(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Link, widgetCapture, { id }, props);
    }
    /**
     * Displays an image
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link ImageWidget}
     */
    image(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(ImageWidget, widgetCapture, { id }, props);
    }
    /**
     * Displays an iframe
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link IFrameWidget}
     */
    iframe(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(IFrameWidget, widgetCapture, { id }, props);
    }
    /**
     * Displays an video player
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link VideoWidget}
     */
    video(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(VideoWidget, widgetCapture, { id }, props);
    }
    /**
     * Displays an audio player
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link AudioWidget}
     */
    audio(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(AudioWidget, widgetCapture, { id }, props);
    }
    /**
     * Renders a line chart
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link LineChart}
     */
    lineChart(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(LineChart, widgetCapture, { id }, props);
    }
    /**
     * Renders a scatter chart
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link ScatterChart}
     */
    scatterChart(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(ScatterChart, widgetCapture, { id }, props);
    }
    /**
     * Renders a bar chart
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link BarChart}
     */
    barChart(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(BarChart, widgetCapture, { id }, props);
    }
    /**
     * @returns The created {@link EChartsWidget}
     */
    echart(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(EChartsWidget, widgetCapture, { id }, props);
    }
    /**
     * Renders a bar chart
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link PieChart}
     */
    pieChart(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(PieChart, widgetCapture, { id }, props);
    }
    /**
     * Renders a progress bar
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link ProgressBar}
     */
    progress(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(ProgressBar, widgetCapture, { id }, props);
    }
    form(id, codeOrProps) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        if (typeof codeOrProps === "function") {
            widgetCapture.setNextProps([{ id, code: codeOrProps, isForm: true }]);
            return new RegionWidget(widgetCapture);
        }
        else {
            widgetCapture.setNextProps([{ id }, codeOrProps]);
            return new Form(widgetCapture);
        }
    }
    region(id, codeOrProps, arg) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        if (typeof codeOrProps === "function") {
            widgetCapture.setNextProps([{ id, code: codeOrProps, arg }]);
        }
        else {
            widgetCapture.setNextProps([{ id }, codeOrProps]);
        }
        return new RegionWidget(widgetCapture);
    }
    accordion(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([
            {
                id,
                title: props.title,
                code: props.code,
                arg: undefined,
                variant: "accordion",
                appearance: props.appearance,
                defaultOpen: props.defaultOpen,
                group: props.group,
            },
        ]);
        return new RegionWidget(widgetCapture);
    }
    carousel(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([
            {
                id,
                title: props.title,
                code: props.code,
                arg: undefined,
                variant: "carousel",
                appearance: props.appearance,
                scrollBehavior: props.scrollBehavior,
                defaultOpen: props.autoPlay,
            },
        ]);
        return new RegionWidget(widgetCapture);
    }
    palette(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([
            {
                id,
                title: props.title,
                code: props.code,
                arg: undefined,
                variant: "palette",
                group: props.group,
                defaultOpen: props.defaultOpen,
            },
        ]);
        return new RegionWidget(widgetCapture);
    }
    card(id, code, arg) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([{ id, code, arg, appearance: "card" }]);
        return new RegionWidget(widgetCapture);
    }
    well(id, code, arg) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([{ id, code, arg, appearance: "well" }]);
        return new RegionWidget(widgetCapture);
    }
    sidebar(code, props) {
        this.throwIfExpired();
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([
            {
                id: "__sm_sidebar",
                code,
                arg: undefined,
                variant: "sidebar",
                appearance: (props === null || props === void 0 ? void 0 : props.appearance) || "well",
            },
        ]);
        return new RegionWidget(widgetCapture);
    }
    repeat(id, props) {
        const { label, appearance, size, renderItem, renderAdd = "+ add", comparator = deepEqual, } = props;
        // this can be re-assigned in the external setVal
        let defaultVal = props.defaultVal || EMPTY_LIST;
        this.throwIfExpired(id);
        // we need this to have a stable value
        const ext = this.memo(`__sm_repeat_${id}`, () => ({
            modified: false,
            setVal: undefined,
            ready: true,
        }), [], {
            modified: false,
            setVal: undefined,
            ready: false,
        }).val;
        // outer region to isolate everything
        const code = (ui) => __awaiter(this, void 0, void 0, function* () {
            const state = ui.getState(() => {
                let seq = 100;
                return {
                    modified: ext.modified,
                    emitted: undefined,
                    given: defaultVal.map((item) => ({ id: seq++, item })),
                    seq,
                };
            });
            const { given, modified, emitted } = state;
            ext.setVal = (list = EMPTY_LIST) => {
                ui.setState(({ seq }) => ({
                    modified: true,
                    emitted: undefined,
                    given: list.map((item) => ({ id: seq++, item })),
                    seq,
                }));
            };
            const list = given.map((x) => x.item);
            if (label || label == 0) {
                ui.txt("label", md ``, { label });
            }
            else if (given.length === 0) {
                // always render something into the region...
                ui.md ``;
            }
            ui.effect("default", () => {
                if (modified)
                    return noop;
                if (comparator(defaultVal, list))
                    return noop;
                ui.setState(({ seq }) => ({
                    given: defaultVal.map((item) => ({ id: seq++, item })),
                    emitted: undefined,
                    seq,
                }));
                return noop;
            }, [defaultVal]);
            const push = (data) => {
                ui.setState(({ given, seq }) => ({
                    modified: true,
                    given: [...given, { id: seq++, item: data || undefined }],
                    seq,
                }));
            };
            const output = given.map(({ id, item }, index) => {
                const del = () => {
                    given.splice(index, 1);
                    ui.setState(({ given }) => ({ modified: true, given: [...given] }));
                };
                // region per row to avoid id collisions
                return ui.region(`row_${id}`, (ui) => __awaiter(this, void 0, void 0, function* () {
                    ui.md ``;
                    const entry = {
                        item,
                        index,
                        list,
                        del,
                    };
                    if (index > 0) {
                        entry.prev = given[index - 1].item;
                    }
                    if (index + 1 < given.length) {
                        entry.next = given[index + 1].item;
                    }
                    const result = yield renderItem(ui, entry);
                    if (!ext.ready)
                        return;
                    if (comparator === deepEqual && isNonValWidget(result)) {
                        console.warn(`ui.repeat(id = ${id}).renderItem returned a non-input widget.  The return value is ignored.`);
                        ui.emit(null);
                    }
                    else {
                        ui.emit(result, comparator);
                    }
                }));
            });
            if (renderAdd) {
                if (typeof renderAdd === "function") {
                    renderAdd(ui, push, list);
                }
                else if (ui.button("add_btn", {
                    label: renderAdd,
                    appearance: "button_muted",
                    size: "xs",
                }).didClick) {
                    push(undefined);
                }
            }
            // NOTE: important to touch every item's .isReady for some reason
            if (output.map((item) => item.isReady).every((x) => !!x)) {
                const vals = output
                    .map((item) => item.val)
                    .filter((val) => val !== OMIT_VALUE);
                // const invalid = vals.some(val => val === INVALID_VALUE);
                if (emitted) {
                    if (comparator(emitted, vals))
                        return;
                    ui.setState({ emitted: vals, modified: true });
                    ui.emit(vals);
                }
                else {
                    // first run
                    ui.setState({ emitted: vals });
                    ui.emit(vals);
                }
            }
        });
        // TODO: handle the case where list is empty and add is false
        const r = this.region(id, {
            code,
            appearance,
            size,
            arg: undefined,
        });
        // access .isReady to cause .val to be exposed
        // eslint-disable-next-line @typescript-eslint/no-unused-expressions
        r.isReady;
        // @ts-ignore
        r.setVal = (val) => {
            if (ext.setVal) {
                ext.setVal(val);
            }
            else {
                ext.modified = true;
                defaultVal = val;
            }
        };
        return r;
    }
    list(id, props) {
        const { appearance, scrollBehavior, renderItem, list = EMPTY_LIST, comparator = deepEqual, size, } = props;
        this.throwIfExpired(id);
        // TODO: support promise, fn etc for list
        // outer region to isolate everything
        const code = (ui) => __awaiter(this, void 0, void 0, function* () {
            // TODO: remove when empty region bug is fixed
            // always render something into the region...
            ui.md ``;
            const output = list.map((item, index) => ui.region(`row_${index}`, (ui) => __awaiter(this, void 0, void 0, function* () {
                const result = yield renderItem(ui, {
                    item,
                    prev: index ? list[index - 1] : undefined,
                    next: index + 1 < list.length ? list[index + 1] : undefined,
                    index,
                    list,
                });
                // TODO: remove when empty region bug is fixed
                ui.md ``;
                if (comparator === deepEqual && isNonValWidget(result)) {
                    console.warn(`ui.list(id = ${id}).renderItem returned a non-input widget.  The return value is ignored.`);
                    ui.emit(null);
                }
                else {
                    ui.emit(result, comparator);
                }
            })));
            // NOTE: important to touch every item's .isReady for some reason
            if (output.map((item) => item.isReady).every((x) => !!x)) {
                ui.emit(output.map((item) => item.val));
            }
        });
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([
            { id, code, appearance, size, scrollBehavior },
        ]);
        return new RegionWidget(widgetCapture);
    }
    /**
     * A simplified version of list that doesn't emit values and lets renderItem handle region creation.
     * Useful when you just want to render a list of items without tracking their values.
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Configuration object containing renderItem function, list of items, and appearance options
     * @returns The created RegionWidget
     */
    simpleList(id, props) {
        const { appearance, scrollBehavior, renderItem, list = EMPTY_LIST, size, } = props;
        this.throwIfExpired(id);
        const code = (ui) => __awaiter(this, void 0, void 0, function* () {
            // TODO: remove when empty region bug is fixed
            ui.md ``;
            list.forEach((item, index) => {
                renderItem(ui, {
                    item,
                    prev: index ? list[index - 1] : undefined,
                    next: index + 1 < list.length ? list[index + 1] : undefined,
                    index,
                    list,
                });
            });
        });
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        widgetCapture.setNextProps([
            { id, code, appearance, size, scrollBehavior },
        ]);
        return new RegionWidget(widgetCapture);
    }
    tabs(id, tabs, props) {
        return this.region(id, (ui) => __awaiter(this, void 0, void 0, function* () {
            var _a;
            const nav = ui.nav(`_sm_tab_${id}`, {
                appearance: (_a = props === null || props === void 0 ? void 0 : props.appearance) !== null && _a !== void 0 ? _a : "tabs",
                options: tabs.map((t) => ({
                    value: t.id,
                    label: t.label,
                    icon: t.icon,
                    disabled: t.disabled,
                })),
                defaultVal: props === null || props === void 0 ? void 0 : props.defaultTab,
            });
            const tab = tabs.find((t) => t.id === nav.val);
            if (tab) {
                const code = ui.region("code", tab.renderTab);
                // this needs to emit .val, .tab and .setTab
            }
            // TODO, can't render empty regions
            ui.md ``;
            ui.emit(nav.val);
        }));
    }
    txt(id, content, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Txt, widgetCapture, { id, content }, props);
    }
    code(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(CodeWidget, widgetCapture, { id }, props);
    }
    editor(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(CodeMirrorWidget, widgetCapture, { id }, props);
    }
    /**
     * A template literal for rendering markdown.
     *
     * @example
     * ui.md`# Hello World!`
     */
    md(strings, ..._) {
        this.throwIfExpired();
        if (typeof strings === "string") {
            // they probably messed up and called ui.md("Hello") instead of ui.md`Hello`
            // TODO: add a line #
            this.warn("Call ui.md as ui.md`...` instead of ui.md(...) or use ui.txt()");
            // @ts-expect-error
            return this.show(md([strings]));
        }
        // @ts-expect-error
        return this.show(md.apply(null, arguments));
    }
    /**
     * Renders the menubar
     *
     * @returns The created {@link MenuBar}
     */
    menubar(props) {
        this.throwIfExpired();
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(MenuBar, widgetCapture, { id: "__sm_menubar" }, props);
    }
    /**
     * Sets page title and theme
     *
     * @returns The created {@link Theme}
     */
    theme(props) {
        this.throwIfExpired();
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Theme, widgetCapture, { id: "__sm_theme" }, props);
    }
    /**
     * Renders a breadcrumb
     *
     * @returns The created {@link Breadcrumb}
     */
    breadcrumb(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Breadcrumb, widgetCapture, { id }, props);
    }
    /**
     * Renders an info bar
     *
     * @returns The created {@link InfoBar}
     */
    infobar(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(InfoBar, widgetCapture, { id }, props);
    }
    /**
     * Renders an icon with a tooltip
     *
     * @returns The created {@link InfoIcon}
     */
    infoIcon(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(InfoIcon, widgetCapture, { id }, props);
    }
    /**
     * Will render the given content to the user. This method is most useful when
     * paired with the {@link md} function.
     *
     * @param data The content to display
     */
    show(data) {
        this.throwIfExpired();
        const { declaredContent } = __classPrivateFieldGet(this, _UI_internals, "f");
        declaredContent.push(typeof data === "string"
            ? { type: "string", data }
            : { type: "md", data: { unparsed: data.unparsed, args: data.args } });
    }
    /**
     * Display a toast msg to the user.
     *
     * @param msg
     * @returns
     */
    toast(msg) {
        assertParseSucceeded(zToastProps.safeParse({ msg }), "<unknown>");
        return __classPrivateFieldGet(this, _UI_internals, "f").regionSession.fireMsg({
            type: "toast",
            data: { msg },
        });
    }
    /**
     * Displays a location search input box
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link LocationSearch}
     */
    locationSearch(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(LocationSearch, widgetCapture, { id }, props);
    }
    /**
     * Displays a Google Map
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link GoogleMaps}
     */
    googleMaps(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(GoogleMaps, widgetCapture, { id }, props);
    }
    /**
     * Async returns user's devices geolocation
     *
     * .isReady is false while waiting for the location OR if there is an error.
     * When .isReady is true, .val will contain the geo location.
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link GeoLocation}
     */
    geoLocation(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(GeoLocation, widgetCapture, { id }, props);
    }
    /**
     * Returns the browser's locale information including timezone, language, and regional settings
     *
     * .isReady is true when locale info is available.
     * When .isReady is true, .val will contain the complete locale information.
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link Locale}
     */
    locale(id = "_get_locale") {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Locale, widgetCapture, { id }, {});
    }
    /**
     * Render a Joystick input
     *
     * @param id A short, descriptive id to reference within the codebase
     * @param props Additional props you may wish to set
     * @returns The created {@link Joystick}
     */
    joystick(id, props) {
        this.throwIfExpired(id);
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(Joystick, widgetCapture, { id }, props);
    }
    /**
     * Returns the current state, setting a default state if necessary
     * @param makeDefault A function that computes the default state
     */
    getState(makeDefault) {
        this.throwIfExpired();
        const { regionSession } = __classPrivateFieldGet(this, _UI_internals, "f");
        let state = regionSession.getState();
        if (!state) {
            state = makeDefault();
            regionSession.setState(state);
        }
        return state;
    }
    pageParams() {
        this.throwIfExpired();
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(PageParams, widgetCapture, {
            id: "__sm_page_params",
            initialVal: __classPrivateFieldGet(this, _UI_internals, "f").params,
        });
    }
    history(props) {
        this.throwIfExpired();
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(History, widgetCapture, {
            id: "__sm_history",
            url: __classPrivateFieldGet(this, _UI_internals, "f").params.url,
            route: __classPrivateFieldGet(this, _UI_internals, "f").params.route,
        }, props);
    }
    // TODO: confirmExit?
    preventExit() {
        this.throwIfExpired();
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(PreventExit, widgetCapture, {
            id: "__sm_prevent_exit",
        });
    }
    router(id, { routes }) {
        const result = z.object({ routes: zRouteMap }).safeParse({ routes });
        if (assertParseSucceeded(result, id)) {
            routes = result.data.routes;
        }
        // wrap everything in a region so we can be placed via our id
        return this.region(id, (ui) => {
            const history = ui.history();
            //@ts-ignore
            const { route, args } = match_path_to_route(history.route, routes);
            // TODO: ui.preventExit
            const render_fn = route ? routes[route] : render_no_route;
            ui.region(route || "__sm_no_route", (inner) => __awaiter(this, void 0, void 0, function* () {
                yield render_fn(inner, args);
            }));
        });
    }
    // path is a relative url (eg: ./apple?foo=1)
    // TODO: validate path
    navigate(link) {
        return __classPrivateFieldGet(this, _UI_internals, "f").regionSession.fireMsg({
            type: "history",
            data: { op: "push", path: zRoute.parse(link) },
        });
    }
    /**
     * This provides the same api as React's
     * [setState](https://reactjs.org/docs/react-component.html#setstate). If
     * provided an object, the individual fields will be applied to the object
     * (and a re-render will be skipped if all fields remain the same). If given a
     * function, then the function will be called with the most-recent state (the
     * updaters may be batched) and the result will be assigned into the current
     * state.
     *
     * @param action Either a partial object that will be applied to the state, or
     * a function that takes the old state and outputs fields to be applied.
     */
    setState(action) {
        return __classPrivateFieldGet(this, _UI_internals, "f").regionSession.setState(action);
    }
    /**
     * Schedules the region to re-render. If it is currently in a render, then it
     * will wait for the render to end and trigger another one.
     */
    rerender() {
        __classPrivateFieldGet(this, _UI_internals, "f").regionSession.regionVals.triggerRender();
    }
    /**
     * Clears out and re-runs all the ui.async results
     */
    clearCaches() {
        return __classPrivateFieldGet(this, _UI_internals, "f").regionSession.regionVals.invalidateCache();
    }
    /**
     * Displays a modal to the user. You should await it's completion.
     *
     * If the user escapes out of the modal (pressing the X button), then the
     * promise will resolve to `undefined`.
     *
     * @param id
     * @param code
     * @returns A promise that will resolve with the argument passed to
     * ui.close(...) or with `undefined` if the user escaped the modal.
     */
    modal(id, code) {
        assertParseSucceeded(zModalProps.safeParse({ id }), id);
        const { regionSession } = __classPrivateFieldGet(this, _UI_internals, "f");
        return rxToPromise(regionSession.runModal(id, code, undefined)).then((result) => {
            regionSession.regionVals.triggerRender();
            return result;
        });
    }
    closeWindow(props) {
        this.throwIfExpired();
        const { widgetCapture } = __classPrivateFieldGet(this, _UI_internals, "f");
        return makeWidget(CloseWindow, widgetCapture, { id: "__sm_close_window" }, props);
    }
    /**
     * Displays a confirm dialog and returns a Promise that resolves to
     * a boolean true/false based on the user's choice.
     *
     * @returns A promise that will resolve with true or false
     */
    confirm(props) {
        const rx = __classPrivateFieldGet(this, _UI_internals, "f").regionSession.runModal(RegionType.Alert, (ui) => {
            const { widgetCapture } = __classPrivateFieldGet(ui, _UI_internals, "f");
            makeWidget(Confirm, widgetCapture, { id: "alert" }, typeof props === "object" ? props : { text: props });
        }, undefined);
        return rxToPromise(rx);
    }
    /**
     * Displays an alert message with a configurable dismiss button.
     *
     * @returns A promise that will resolve once the alert has been dismissed.
     */
    alert(props) {
        const rx = __classPrivateFieldGet(this, _UI_internals, "f").regionSession.runModal(RegionType.Alert, (ui) => {
            const { widgetCapture } = __classPrivateFieldGet(ui, _UI_internals, "f");
            makeWidget(Alert, widgetCapture, { id: "alert" }, typeof props === "object" ? props : { text: props });
        }, undefined);
        return rxToPromise(rx);
    }
    launch(id, code, arg) {
        assertParseSucceeded(zLaunchProps.safeParse({ id }), id);
        __classPrivateFieldGet(this, _UI_internals, "f").regionSession.setSubregion(RegionType.Launched, id, code, arg);
    }
    overlay(code, arg) {
        return rxToPromise(__classPrivateFieldGet(this, _UI_internals, "f").regionSession.runModal(RegionType.Cover, code, arg));
    }
    /**
     * Logs the data to the console and also shows it to the client.
     */
    log(...data) {
        __classPrivateFieldGet(this, _UI_internals, "f").regionSession.log(LogLevel.Log, data);
    }
    warn(...data) {
        __classPrivateFieldGet(this, _UI_internals, "f").regionSession.log(LogLevel.Warn, data);
    }
    /**
     * Shows an error in the browser (used like console.error(...))
     */
    error(...data) {
        __classPrivateFieldGet(this, _UI_internals, "f").regionSession.log(LogLevel.Error, data);
    }
    /**
     * Ends the modal or region with the given value.
     * @param result The value to return
     */
    close(result) {
        if (__classPrivateFieldGet(this, _UI_internals, "f").expired) {
            const session = __classPrivateFieldGet(this, _UI_internals, "f").regionSession;
            if (session.isComplete) {
                throw new SkyMassUIError(`modal(id = ${session.regionRefId}).close(result=${result}) was called after the modal was already closed`);
            }
            else {
                session.complete(result);
            }
        }
        else {
            __classPrivateFieldGet(this, _UI_internals, "f").result = { value: result };
        }
    }
}
_UI_internals = new WeakMap();
/////////////////////////////////////////////////////////////////////////////
// internally exposed
/////////////////////////////////////////////////////////////////////////////
/**
 * Builds out a UI object to be used for one render-round.
 *
 *
 * @returns Both the public ui object and the private-to-the-api internals
 */
export function buildApi(regionSession, onValDeclared) {
    const declaredContent = [];
    const internals = {
        expired: false,
        regionSession,
        declaredContent,
        user: regionSession.user,
        params: regionSession.params,
        result: undefined,
        widgetCapture: null,
    };
    // @ts-expect-error this is the only place where the private constructor
    // should be used
    const ui = new UI(internals);
    internals.widgetCapture = new WidgetCapture(ui, regionSession, onValDeclared, (collect) => {
        declaredContent.push({ type: "builder", data: collect });
    });
    return [ui, internals];
}
function render_no_route(ui) {
    // TODO: log?
    ui.md ``;
}
// TODO: validate routes
// /(token/|param/)+\*?$
// "/apple/:id" -> "/apple/(?<id>[^/]+)"
// convert a /apple/:id/* -> regex with named capture groups
function pattern_to_re(pattern = "") {
    pattern = pattern
        .replaceAll(/:([a-z_]+)/g, "(?<$1>[A-Za-z0-9_.!~*'()\\- %]+)")
        .replace(/\*$/, "(?<_slug>[^?#]+)$");
    return `^${pattern}$`;
}
function match_path_to_route(path, routes) {
    for (const route of Object.keys(routes)) {
        // TODO: cache this?
        const re = pattern_to_re(route);
        const match = new RegExp(re).exec(path);
        if (match) {
            const decodedArgs = Object.fromEntries(Object.entries(match.groups || {}).map(([k, v]) => [
                k,
                decodeURIComponent(v),
            ]));
            return { route, args: decodedArgs };
        }
    }
    if (routes["/*"]) {
        return { route: "/*", args: {} };
    }
    else if (routes["/"]) {
        return { route: "/", args: {} };
    }
    return { route: null, args: {} };
}
const zMemoProps = z.object({
    id: zWidgetId,
    code: z.function(),
    dependencies: z.array(z.any()),
    initialVal: z.any().optional(),
});
const zEffectProps = z.object({
    id: zWidgetId,
    effect: z.function(),
    dependencies: z.array(z.any()),
});
const zSubscribeProps = z.object({
    id: zWidgetId,
    code: z.function(),
    dependencies: z.array(z.any()),
    transform: z.function().optional(),
    initialVal: z.any().optional(),
});
const zLaunchProps = z.object({
    id: zWidgetId,
});
const zToastProps = z.object({
    msg: z.string(),
});
const zModalProps = z.object({
    id: zWidgetId,
});
