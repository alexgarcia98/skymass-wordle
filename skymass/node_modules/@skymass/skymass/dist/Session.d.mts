import { CreateRegion } from "./RegionSession.mjs";
import { Observer, OperatorFunction } from "rxjs";
import { C2PMsg, P2CMsg, UserDetails } from "./protocol.mjs";
import { PageInfo } from "./ProducerManager.mjs";
export type SessionParams = {
    url: string;
    route: string;
};
/**
 * Represents an active browser connection.
 */
export declare class Session {
    readonly user: null | UserDetails;
    readonly params: SessionParams;
    /**
     * This is the value the server uses to identify this session
     */
    readonly sessionId: string;
    /**
     * We assign each region a unique id
     */
    private nextRegionId;
    /**
     * The initial page that opened this session
     */
    readonly entrypoint: PageInfo;
    private subscription;
    /**
     * A map from region ids to their regions
     */
    private readonly regions;
    private readonly sidechannelSubject;
    private readonly sidechannels;
    private readonly initialUrl;
    private constructor();
    static create(sessionId: string, page: PageInfo, user: null | UserDetails, params: SessionParams, initialUrl: URL): Session;
    start<T>(op: OperatorFunction<P2CMsg, T>, observer: Partial<Observer<T>>): void;
    stop(): void;
    readonly createRegion: CreateRegion;
    handleClientEvents(events: C2PMsg[]): void;
}
