import { Observable } from "rxjs";
import { WebSocket } from "ws";
import { BackoffConfig } from "./operators/backoff.mjs";
export type TypedSocket<OutT> = {
    sendJson(msg: OutT): void;
    ws: WebSocket;
};
type ActiveSocketNotification<InT, OutT> = {
    type: "open";
    data: {
        iteration: number;
        socket: TypedSocket<OutT>;
    };
} | {
    type: "msg";
    data: InT | ArrayBuffer;
};
type SocketNotification<InT, OutT> = ActiveSocketNotification<InT, OutT> | {
    type: "close";
};
export declare function socketRx<InT, OutT>(backoffConfig: BackoffConfig, socketUrl: string): Observable<SocketNotification<InT, OutT>>;
export {};
